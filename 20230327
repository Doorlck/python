2023 0327 
1. 파이썬은 부동소수점 표현으로 실수를 표현한다. 
2. 연산 순서: 괄호 -> 지수 -> 곱, 나눗셈, 나머지 -> 더하기 빼기
3. input() 은 입력값을 모두 문자열 형태로 저장
4. int() 에 넣거나 몫을 알려주는 // 연산자를 통해 실수를 정수로 바꿀 수 있음
5. map 에 int 와 input().split()을 넣으면 split의 결과를 모두 int로 변환해줌
변수1, 변수2 = map(int, input().split())
6. print(1,2,3,sep='') 
7. == 는 값을, is는 객체를 비교 
8. and, or, not 논리 연산자가 한 식에 들어있으면 not, and, or 순으로 판단함.
